<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="20dp"
    >

    <ImageView
        android:id="@+id/image_inside_folder"
        android:layout_width="match_parent"
        android:scaleType="fitCenter"
        android:layout_height="200dp"/>

    <CheckBox
        android:id="@+id/inside_folder_check_box"
        android:visibility="invisible"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/image_inside_folder"
        android:layout_alignBottom="@+id/image_inside_folder"
        android:layout_marginRight="21dp"
        android:layout_marginBottom="39dp" />

    <TextView
        android:id="@+id/image_page_number"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/image_inside_folder"
        android:layout_gravity="center"
        android:layout_marginTop="-30dp"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:padding="5dp"
        android:background="@color/colorBlack"
        android:alpha=".7"
        android:text="Page Number"
        android:textColor="@color/colorWhite"
        android:textStyle="bold"></TextView>

</RelativeLayout>